export const metadata = {
  title: 'فیوچرلنز | جزئیات تحلیل',
  description: 'جزئیات وضعیت و روند اجرای تحلیل'
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
