- فایل‌های ورودی فعلاً به صورت متن UTF-8 پردازش می‌شوند؛ استخراج تخصصی PDF/DOCX در گام بعدی جایگزین می‌شود.
- ماژول‌های ۱ تا ۷ در حال حاضر بر پایه منطق قطعی و ساده پیاده شده‌اند تا اسکلت MVP فعال باشد؛ اتصال کامل به LLM با ساختار چندمرحله‌ای روی همین ماژول‌ها اعمال خواهد شد.
- ارسال پاسخ به سوالات روشن‌سازی با endpoint `/api/jobs/:id/clarifications` باعث اجرای مجدد کل پایپ‌لاین با همان ورودی و پاسخ‌های جدید می‌شود.
